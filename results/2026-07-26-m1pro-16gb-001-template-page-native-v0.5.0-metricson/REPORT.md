---
title: "2026-07-26-m1pro-16gb-001-template-page-native-v0.5.0-metricson"
scenario: "001-template-page"
target: "native"
octoVersion: "0.5.0"
date: 2026-07-26T17:26:22Z
---

# 2026-07-26-m1pro-16gb-001-template-page-native-v0.5.0-metricson

Scenario **001-template-page** on target **native**, Octo **0.5.0**, captured 2026-07-26T17:26:22Z.

> Generated by `lab/bin/report.py`. Do not edit by hand — re-run the benchmark instead.

## Environment

| | |
|---|---|
| Host profile | `m1pro-16gb` |
| CPU | Apple M1 Pro (arm64) |
| Cores | 10 logical — 8 performance + 2 efficiency |
| Memory | 16.0 GiB |
| OS | macOS 26.3.1 (25D771280a) |
| Octo version under test | **0.5.0** |
| Build channel | release |
| Runtime artifact | `/Users/juancavallotti/.octo-versions/octo-0.5.0` (45.8 MiB) |
| k6 | 2.1.0 |
| Runtime observability | probes, metrics on the admin port |
| Readiness detected by | `/readyz` |
| Lab commit | `2a39e69` (dirty) |

*The process reported itself as Octo 0.5.0, services module `standalone`, with 1 connector(s) and 1 flow(s) loaded.*

> **Load generator shares this host with the server under test.** k6 and Octo compete for the same cores, which compresses the absolute ceiling. Both variants pay this cost equally, so the baseline↔tuned comparison stands; treat absolute numbers as a floor.

## Runtime footprint

What the runtime costs before it serves a single request.

| | |
|---|---|
| Artifact size | 45.8 MiB |
| Cold start (launch → first 200) | 183 ms |
| Idle RSS (after 30s idle) | 29.5 MiB (peak 38.8 MiB) |
| Idle CPU | 0.11% (peak 1.00%) |
| Idle heap in use | 6.4 MiB |
| Idle goroutines | 27.0 (peak 28) |
| Idle allocation rate | 81.4 KiB/s |

*Cold start is launch → `/readyz` reporting `ready`, meaning every connector and flow started. Runs recorded before the runtime had an admin port measured launch → first 200 from a business route, which is a later moment; the two are not directly comparable.*

*A non-zero idle allocation rate is partly the metrics scrape itself: rendering the exposition allocates, once per sample interval, on the observed process. It is a floor on what the runtime allocates while serving nothing, not a measurement of it.*

## Results

Test `steady`, 3 repetition(s) per variant, median repetition shown. Offered rate 16,000 req/s for 61s.

**Baseline** — `workers`, `buffer`, `pool` stripped from the config, so the runtime uses whatever it defaults to.  
**Tuned** — workers `128`, buffer `256`, pool `8`.

### Validity

| Check | Baseline | Tuned | |
|---|---|---|---|
| Dropped iterations | 358,033 | 470,471 |  |
| Failed requests | 0.000% | 0.000% |  |
| k6 threshold exit | breach (99) | breach (99) |  |
| Messages failed (server) | 0 | 0 |  |
| Messages dropped (server) | 0 | 0 |  |
| Ready throughout window | yes | yes |  |

> ⚠ non-zero dropped iterations — the offered rate exceeded what the server could start, so these latency figures describe a **saturated** system.

### Throughput and latency

| Metric | Baseline | Tuned | Change |
|---|---|---|---|
| Achieved throughput | 9,897.9 req/s | 7,598.2 req/s | ▼ -23.2% |
| Requests | 599,680 | 460,206 |  |
| Latency p50 | 272.51 ms | 339.01 ms | ▼ +24.4% |
| Latency p90 | 725.63 ms | 1,185.11 ms | ▼ +63.3% |
| Latency p95 | 933.01 ms | 1,501.99 ms | ▼ +61.0% |
| Latency p99 | 1,367.47 ms | 2,113.51 ms | ▼ +54.6% |
| Latency max | 3,001.99 ms | 4,593.84 ms | ▼ +53.0% |
| Server think time p95 | 888.16 ms | 1,459.52 ms | ▼ +64.3% |
| Data received | 39.58 MiB/s | 30.38 MiB/s |  |
| Peak VUs | 6,794 | 6,704 |  |

### Resource cost

Measured over the load window only, so start-up and warm-up are excluded.

| Metric | Baseline | Tuned | Change |
|---|---|---|---|
| **CPU-ms per request** | 0.070 ms | 0.081 ms | ▼ +14.8% |
| Requests per CPU-second | 14,194 | 12,364 | ▼ -12.9% |
| CPU consumed | 42.25 s | 37.22 s |  |
| CPU mean | 69.1% | 61.9% |  |
| CPU peak | 153.8% | 136.5% |  |
| RSS mean | 232.0 MiB | 213.2 MiB |  |
| RSS peak | 343.3 MiB | 303.1 MiB | ▲ -11.7% |
| RSS drift over run | 121.0 MiB | 3.5 MiB |  |
| RSS per 1k req/s | 34.7 MiB | 39.9 MiB | ▼ +15.0% |

*100% CPU = one fully saturated core.*

### What the runtime said about itself

Read from the process's own `/metrics` over the same window, by differencing the scrape at the start of the load against the one at the end. These are not a second opinion on the numbers above — they answer questions the outside view cannot reach.

#### Saturation

| Metric | Baseline | Tuned | Change |
|---|---|---|---|
| In-flight mean | 3.73 | 26.09 |  |
| In-flight peak | 8 | 127 |  |
| Goroutines peak | 11,718 | 9,848 |  |
| Open file descriptors peak | 6,805 | 6,714 |  |

`octo_flow_in_flight` is how many messages a flow is processing at the instant of the scrape. It is the difference between a flow that is quiet and one that is stuck: pinned at the worker count with flat throughput means every worker is blocked on something, which from outside the process looks like an idle service.

#### Latency, server-side

| Metric | Baseline | Tuned | Change |
|---|---|---|---|
| Flow duration mean | 0.41 ms | 4.70 ms | ▼ +1041.4% |
| Observations | 599,508 | 460,081 | |
| Flow duration p50 | < 5.00 ms | < 5.00 ms | |
| Flow duration p95 | < 5.00 ms | < 5.00 ms | |
| Flow duration p99 | < 5.00 ms | ~104.02 ms | |

> **The percentiles above are bounded, not measured.** Octo's flow-duration histogram uses Prometheus's default buckets, whose lowest edge is 5.00 ms — and this flow completes well inside it, so every observation lands in the first bucket. Interpolating there yields a number that is a function of the bucket width and nothing else, so it is reported as a bound. **The mean is exact** (a counter sum over a counter count) and is the server-side latency figure to use for this scenario.

Client p95 was 933.01 ms while the runtime's mean flow duration was 0.41 ms. **That gap is queueing at the load generator, not work in the runtime** — this run shed iterations, so k6's latency includes time requests spent waiting to be issued. Two figures that used to be indistinguishable from a slow server are now separable: the runtime was fast and the offered rate was too high.

#### Cost, from inside the process

| Metric | Baseline | Tuned | Change |
|---|---|---|---|
| CPU-ms per message | 0.070 ms | 0.082 ms | ▼ +16.0% |
| Allocated per message | 23.3 KiB | 23.1 KiB | ▲ -0.8% |
| Allocated over window | 13.3 GiB | 10.1 GiB |  |
| GC cycles | 229 | 194 |  |
| GC time | 7,728.5 ms | 7,456.9 ms | ▲ -3.5% |
| Heap in use peak | 198.8 MiB | 196.3 MiB |  |

**Allocation per message is the number to watch when tuning.** A knob that buys throughput by allocating more per message has moved the cost rather than removed it, and RSS will not show that while the collector keeps up.

*CPU over the window: 42.25 s sampled from outside the process, 42.24 s reported by it (-0.0%). The two are measured by different instruments over slightly different intervals; a large divergence is worth explaining before either number is published.*

### Repetition spread

| Variant | Rep | Achieved req/s | p95 | CPU-ms/req | Peak RSS |
|---|---|---|---|---|---|
| baseline | 1 | 14,513.3 | 137.02 ms | 0.098 | 168.9 MiB |
| baseline ←median | 2 | 9,897.9 | 933.01 ms | 0.070 | 343.3 MiB |
| baseline | 3 | 8,598.2 | 1,142.16 ms | 0.073 | 359.4 MiB |
| tuned | 1 | 8,073.4 | 1,395.39 ms | 0.079 | 284.9 MiB |
| tuned ←median | 2 | 7,598.2 | 1,501.99 ms | 0.081 | 303.1 MiB |
| tuned | 3 | 7,273.2 | 1,729.36 ms | 0.076 | 369.3 MiB |

---

Read [METHODOLOGY.md](../../METHODOLOGY.md) for metric definitions and caveats. Raw artifacts for every repetition are in this directory.
